###
# Generated via:
# prefect deployment build src/ingestion/prefect_flows/kafka_to_minio.py:kafka_to_minio_flow -n product-cdc -q default -o orchestration/deployments/kafka_to_minio.yaml
###
deployment:
  name: product-cdc
  flow_name: kafka_to_minio_flow
  parameters:
    topic: "cord.dbo.Product"
    bucket: "cord-raw"
    prefix: "product/raw"
    max_records: 1000
  schedules: []
  tags:
    - ingestion
  infrastructure:
    type: process
    env:
      PREFECT_LOGGING_LEVEL: INFO
      KAFKA_BOOTSTRAP_SERVERS: ${KAFKA_BOOTSTRAP_SERVERS}
      MINIO_ENDPOINT: ${MINIO_ENDPOINT}
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY}
      MINIO_RAW_BUCKET: ${MINIO_RAW_BUCKET}
  pull:
    - prefect.filesystem
